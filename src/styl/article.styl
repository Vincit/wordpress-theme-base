.single-article {
  border-bottom: 1px solid colors-gray
  padding-bottom: r(20px)
  margin-bottom: r(20px)

  &:last-of-type {
    border-bottom: 0
  }
}

.article {
  &-header {

  }

  &-content {
    overflow: hidden
  }

  &-footer {

  }

  &-pagination {
    p {
      font-size: 0
    }

    a {
      font-size: r(20px)
    }
  }
}
